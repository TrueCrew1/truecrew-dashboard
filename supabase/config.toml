# Local CLI label only — distinguishes Supabase projects on the same host during `supabase start`.
# This is NOT the remote project ref. Remote ref = subdomain of SUPABASE_URL (e.g. fmomafwhcuothygmuiwa).
project_id = "true-crew"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323
