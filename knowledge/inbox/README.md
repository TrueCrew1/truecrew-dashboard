# Inbox

Temporary raw captures waiting to be processed into a `sources/`, `concepts/`,
`projects/`, or `decisions/` page. This folder should usually be empty or near-empty —
things pass through it, they don't live here.

This placeholder file exists only so the folder is tracked by git; delete it once the
first real capture lands, or leave it — either is fine.
