# Findings

Synthesized research findings (from interviews, field notes, or cited research).

Copy `../../templates/finding-template.md` → `YYYY-MM-DD-short-slug.md`.
`type: finding`. Set `truth_level` honestly (hypothesis vs validated).

Do not treat a finding as product policy until it is promoted into a
`decisions/` or trusted `concepts/` page.
