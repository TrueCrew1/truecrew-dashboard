# Workflow observations

Field / ops workflow observations (how work actually runs on site or in the shop).

Copy `../../templates/workflow-observation-template.md` → `YYYY-MM-DD-short-slug.md`.
`type: workflow_observation`.

Note ICP / role when known, steps observed, friction, and TrueCrew implications.
Tag regulatory scope under `regs` when relevant.
