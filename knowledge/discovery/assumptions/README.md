# Assumptions

Explicit product / market / workflow assumptions under test.

Copy `../../templates/assumption-template.md` → `YYYY-MM-DD-short-slug.md`.
`type: assumption`. Keep `truth_level: hypothesis` until evidence upgrades it.

Assumptions are **not** policy. Agents must not treat them as settled product truth.
