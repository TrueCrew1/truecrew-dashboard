import type { ApprovalCard } from "./types";

/**
 * Demo PR-pattern cards used to live here. Removed — Approvals only shows
 * live/derived sources (research start, monitor, planner, M&S missions,
 * command-created proposals). Keep this module as the extension point when a
 * real GitHub PR → ApprovalCard mapper lands under `src/lib/api/`.
 */
export const MOCK_PR_APPROVAL_CARDS: ApprovalCard[] = [];
