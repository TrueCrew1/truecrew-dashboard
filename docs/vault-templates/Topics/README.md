# Topics

Theme notes that gather multiple `Sources/` notes under one heading.

Created or updated when triage sees the same theme on more than one file
(e.g. `field-ops`, `approvals`, `second-brain`).
