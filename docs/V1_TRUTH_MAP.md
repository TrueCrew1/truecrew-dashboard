# V1 Truth Map

Capability truth status for True Crew V1. Each row ties evidence on disk to merge
governance labels per [Truth × Merge — Decision Table (V1)](./SYSTEM_LAW_TRUTH_MERGE_DECISION_TABLE_V1.md).

## Chief

| Capability | Status | Evidence required | Known gaps | Phase 1 action |
|---|---|---|---|---|
| Chief approval policy (confidence ≥0.9 + checklist + evidence + risk gating; PR #156 `cursor/chief-confidence-approval-policy-ba4a`) | REAL | `src/components/chief/chiefApprovalPolicy.ts` (threshold + gating + refinement disposition); `src/components/chief/types.ts` (routing + missing-signal/evidence model); `src/components/chief/approvalStatus.ts` (`isForwardedPendingApproval`, forwarded-only stale/pending logic); `src/components/chief/ApprovalBoard.tsx` (forwarded-only decision lane + refinement lane + split counts); `src/components/chief/ChiefPanel.tsx` (forwarded-only actionability); `src/components/chief/ChiefApprovalsContext.tsx` (forwarded-only `pendingApprovalCount`); `tests/chief-approval-policy.test.ts`, `tests/chief-approval-status.test.ts`, `tests/chief-approval-board-actionability.test.ts` | None material at V1 scope — refinement items are visible but clearly marked non-actionable and excluded from approval metrics; forwarded-only contract holds across policy, UI, and counts. | MERGE NOW under Merge Governance V1. |

PR #156 now satisfies REAL → MERGE NOW under Truth × Merge: the policy, UI, and metrics all treat only forwarded pending approvals as operator-actionable, while refinement-routed proposals are visible but explicitly non-actionable and excluded from pending counts. Chief’s operator lane now accurately reflects what the system has accepted as ready for decision.

## Agents

| Capability | Status | Evidence required | Known gaps | Phase 1 action |
|---|---|---|---|---|
| Approved Project Summary → Build Handoff (`research:project-summary-handoff`) | REAL | Chief approval triggers a real Research mission (`research:project-summary-handoff`) that loads workflow context from Supabase, calls the live Research LLM lane, writes Obsidian handoff artifacts + mission JSON + Build Log output, and surfaces truthful status in Builds and Chief → Agents. Code: `src/components/chief/researchProjectSummaryHandoff.ts`, `src/components/chief/ChiefPanel.tsx` (approve → POST), `api/research/project-summary-handoff.ts`, `lib/research/projectSummaryHandoff.ts`, `lib/missions/projectSummaryHandoffStore.ts`, `src/pages/BuildsPage.tsx`, `src/components/chief/AgentWorkBoard.tsx`; tests: `tests/project-summary-handoff.test.ts`, `tests/api-research-project-summary-handoff.test.ts`. Outputs on success: `Operations/Handoffs/Project Summary Handoff — <title>.md`, `Operations/Handoffs/<missionId>.json`, `Operations/Missions/project-summary-handoff/<missionId>.json`, Build Log via `logBuild()`. | Env-gated: requires live API, internal auth, Supabase, Obsidian vault path, and Azure LLM configuration. Mission state is stored in vault JSON rather than a Supabase mission table. Slack notifications, retries, background queueing, and broader mission framework remain out of scope. Failure behavior: missing project → `failed`; Supabase / LLM / vault issues → `blocked` with error text; never `completed` unless writes succeed. | Count as a real V1 governed vertical slice. Use this as the reference end-to-end proof that approvals can trigger agent work, produce durable artifacts, and report truthful status. |
| Monitor incident → Research postmortem (`research:monitor-incident-postmortem`) | REAL | Monitor incident card → Chief approval triggers `POST /api/research/monitor-incident-postmortem`; Research lane loads incident context from Supabase (`fetchIncidentContext`), writes postmortem note + artifact JSON + mission record + Build Log on completed only; execution feedback and result links on approval cards mirror the handoff loop. Code: `src/components/chief/researchIncidentProposal.ts`, `researchMonitorIncidentPostmortem.ts`, `api/research/monitor-incident-postmortem.ts`, `lib/research/monitorIncidentPostmortem.ts`, `lib/missions/monitorIncidentPostmortemStore.ts`; tests: `tests/monitor-incident-postmortem.test.ts`, `tests/api-research-monitor-incident-postmortem.test.ts`. Outputs on success: `Operations/Handoffs/Monitor Incident Postmortem — <title>.md`, `Operations/Handoffs/<missionId>-postmortem.json`, `Operations/Missions/monitor-incident-postmortem/<missionId>.json`, Build Log via `logBuild()`. | Same env gates as handoff. Incident must exist in Supabase `incidents` table. No Slack, retries, or background queue. Mock mode does not fabricate artifacts or completed status. Today approval-activity UI deferred. | Second concrete governed Research mission slice — parallel pattern to project-summary-handoff. |

Approved Project Summary → Build Handoff and Monitor incident → Research postmortem are the two concrete governed Research mission slices in V1: a real approval creates a real Research mission, the mission runs against real Supabase context, writes durable Obsidian artifacts and Build Log output, and reports honest state back into the product. Today/Home exposes live mission activity (`AgentMissionsCard` on `TodayPage.tsx`). Chief’s home panel includes an agent status strip (`AgentStatusStrip` on `ChiefHomePanel.tsx`) built from real mission hooks, build-gate signals, librarian filing candidates, and Monitor health probes. Chief’s situation brief (`ChiefSituationBrief`) surfaces monitor-derived platform state via `deriveChiefSituationBriefFromMonitor`, using the same `useMonitorHealth` hook as the Monitor page. Live probe failures appear as a single governed approval card via `deriveMonitorApprovalCards` in `ChiefApprovalsContext`. Approval decisions persist activity snapshots to vault JSON (`Operations/Approvals/activity/`) when live API + vault are configured. Chief approval cards show post-decision execution feedback tied to real mission state (`deriveApprovalExecutionFeedback`). Completed missions surface durable output paths on approval cards (`deriveApprovalResultLinks`). Today approval-activity UI and Chief deep-link navigation are deferred.

## File / Second Brain

| Capability | Status | Evidence required | Known gaps | Phase 1 action |
|---|---|---|---|---|
| Drive-only workspace triage + setup (`workspace:setup` + `workspace:triage`) | BLOCKED | `scripts/setup-truecrew-workspace.ts:65-110` (TDZ fixed, calls `setupWorkspace`); `lib/workspace/setup.ts:63-132` (Drive folder tree, permissions doc, CSV log seed, vault folders); `scripts/triage-inbox.ts:95-142` (gates on workspace presence, runs triage); `lib/workspace/triage.ts:34-200` (classify, move, log, topic/synthesis note creation when vault configured); `tests/workspace-triage.test.ts:84-217` (setup + move/log/note flow, dry-run behavior, synthesis drafts) | Hardcoded founder-machine defaults (`lib/workspace/paths.ts:6-9`) and no validation that the path is an actual mounted/synced Google Drive — setup will `mkdir` whatever path resolves. Runtime depends on env/path correctness (`lib/workspace/config.ts:32-39`, `lib/obsidian/config.ts:33-40`), and Obsidian outputs are skipped when vault is missing (`lib/workspace/triage.ts:56-57,103-107`; `scripts/triage-inbox.ts:129-135`). | BLOCKED until env/path hardening — keep implementation, but add mount/path validation and clearer preflight guarantees before marking REAL. |
| Obsidian rolling-log / decision / hot-context note writing (`lib/obsidian/*`) | REAL — verified running on this machine | `lib/obsidian/write.ts:21-45` (real `fs.promises` I/O); `lib/obsidian/write.ts:5-15` (path-traversal guard); `lib/obsidian/templates.ts:38-44` (build-log template) and decision note path/frontmatter; `lib/obsidian/sync.test.ts` (unit tests exercising real fs against temp dirs); direct vault evidence: `TRUE CREW-SECOND BRAIN/Operations/Logs/Build Log.md` with 56 `##` entries matching template fingerprints, and Decisions/*.md files matching decisionNotePath naming and frontmatter. | None material — this path is proven: real I/O, guarded paths, tests, and live vault artifacts. | Keep as REAL — Obsidian log/decision notes are an operational capability in V1. |
| Librarian task-artifact note generation (`lib/librarian/*`) | REAL code, unverified in practice | `lib/librarian/create.ts:36-38` (requires Supabase configured, throws if not); `lib/librarian/create.ts:58-62` (calls `writeVaultNote` but silently sets `vaultWritten: false` with no error/log if vault isn’t configured); `lib/librarian/types.ts:29-46` (note types: build/deploy/incident/ticket/decision/onboarding); shared primitives with Obsidian writer via `writeVaultNote`. | Target folder `Operations/Artifacts/` does not exist in the real vault; there is no evidence this path has ever executed successfully on this machine. Silent skip-on-unconfigured-vault is a real failure mode. | Treat as REAL implementation but unproven; Phase 1 action: do not present as proven. Plan one real smoke test (e.g., POST `/api/librarian/artifacts` against a real task with live API + Supabase) and confirm a file lands in `Operations/Artifacts/` before promoting this path to “REAL in practice.” |
| “Sources / Topics / Synthesis” note taxonomy (automated) | NOT STARTED / NOT IMPLEMENTED (no stub exists) | No `lib/workspace/second-brain.ts`, no Sources/Topics/Synthesis templates, no vault folders or code paths using those names. Closest analog is `knowledge/` — a separate, git-tracked markdown second brain (sources/, concepts/, projects/, decisions/, lessons/) described in `knowledge/README.md:58-69` as manual/agent-edited. | Task premise conflated the repo-internal `knowledge/` convention with the Obsidian/Librarian pipeline; they are unrelated systems. There is no automated Sources/Topics/Synthesis capability today. | Either drop this as an automated capability row or rewrite it to describe `knowledge/` as a manual/agent-curated convention, not a code-generated feature. For Phase 1, mark as NOT STARTED / NOT IMPLEMENTED. |
| Drive → Obsidian triage linkage | NOT STARTED / NOT BUILT | No `lib/workspace/`, no triage.ts touching note creation, and no Google Drive ingestion code. Only adjacent artifact is `docs/internal/drive_truecrew_export.md`, a manual runbook for exporting Drive files to AnythingLLM, unrelated to Obsidian. | This linkage is fiction at current code state; no triage or import path exists to connect Drive to Obsidian. | Mark as NOT STARTED / NOT BUILT — nothing to verify because nothing exists. Treat as future scope, not current capability. |
| Frontend demo-mode artifact mock (`src/lib/librarian/mockCreate.ts`) | MOCK — correctly labeled and gated | `src/lib/librarian/mockCreate.ts:14-59` (pure in-memory function, no disk I/O); `src/context/DataContext.tsx:137-144` uses `mockCreate` only when `isLiveApiEnabled()` is false, otherwise uses the real API. | None — this is a legitimate, well-isolated demo mock path, not a disguised stub. | No action needed in Phase 1; it’s already clearly MOCK and gated to demo mode. |

Obsidian log/decision notes are unambiguously REAL and proven by direct filesystem evidence, unit tests, and template fingerprints in the live vault. Librarian artifact notes share the same primitives but have no evidence of successful end-to-end runs and silently skip work when the vault is unconfigured, so they are REAL implementation but unverified in practice and must not be presented as proven until a smoke test writes to `Operations/Artifacts/`. The “Sources/Topics/Synthesis” taxonomy and any Drive→Obsidian triage linkage are simply not implemented — they belong in future scope, not as current capabilities. The demo-only mock path is correctly marked MOCK and does not threaten operational honesty.

## Integrations

| Capability | Status | Evidence required | Known gaps | Phase 1 action |
|---|---|---|---|---|
| Slack Manual Approval / Command Surface | REAL (workspace + app installed; human-only) | External: `true crew ops` Slack app installed in Truecrew LLC, bot present in `#truecrew-chief`, incoming webhook authorizations for that channel. Repo: Slack only appears in docs as a future/manual tool; no code path consumes Slack messages. | Dashboard does not ingest or act on Slack messages. All real approval authority is the in-app Chief Approval Panel (card-based), per `docs/AGENT_WORKFLOW.md`. | V1 may rely on Slack as a human coordination surface (operators discussing or echoing decisions in `#truecrew-chief`), but the product does not consider a Slack message to be an executed approval or command. |
| Slack Automated Notifications / Turnover | NOT STARTED | Repo: no `SLACK_` env vars, no webhook/token usage, no Slack SDK, and no notification/turnover code paths. Docs: `docs/AGENT_RUNBOOK.md` defers daily/weekly briefs to "Slack once wired"; `docs/TOOL_CATALOG.md` and `knowledge/reference/tool-fallbacks.md` classify Slack as future-integration/manual-only. | Integration path is completely unbuilt: no `SLACK_WEBHOOK_URL`, no `postToSlack()` helper, no routes/jobs sending Chief digests or turnover messages. External infra is ready, but the app does not use it yet. | Keep V1 honest as NOT STARTED for automated Slack. Phase 1 candidate: add env (`SLACK_WEBHOOK_URL`) and a thin, approval-gated digest sender for Chief's daily/weekly briefs; until then, all automated notifications remain in-app only. |
| Integration Health (Monitor) | REAL (env-gated; mock mode when `VITE_USE_LIVE_API` is false) | `api/monitor/index.ts` — `target=vercel`: live `fetch` to `https://api.vercel.com/v6/deployments` (`:78-124`, needs `VERCEL_API_TOKEN` + `VERCEL_PROJECT_ID`; missing → `ok: false`, `"Vercel monitor is not configured"`). `target=supabase`: live `supabase.rpc("monitor_supabase_health")` (`:46-75`, migration `supabase/migrations/20260703000001_monitor_supabase_health.sql`); unconfigured DB → `503`. `src/hooks/useMonitorHealth.ts` — polls both every 45s when `isLiveApiEnabled()` (`:73-90`); skips fetch in mock mode (`:74-79`). UI: `src/pages/MonitorPage.tsx` (`PlatformHealthCard` for Vercel/Supabase, `:124-140`); `src/components/chief/ChiefSituationBrief.tsx` (`vercelIssueLabel` / `supabaseIssueLabel`, `:20-40`, `:78-83`). Config probe (not live ping): `api/health.ts` — `supabase: isSupabaseConfigured()`, `githubWebhook: Boolean(GITHUB_WEBHOOK_SECRET)` (`:8-14`); `src/pages/SettingsPage.tsx` Integrations table (`:76-154`). | `VERCEL_API_TOKEN` / `VERCEL_PROJECT_ID` not in `.env.example` (only in `docs/DEPLOY_NOW.md` / GH Actions secrets). Vercel monitor handler has no `requireInternalAuth` (unlike Supabase). No GitHub Actions / CI status in Monitor. `/api/health` checks webhook secret presence, not delivery. No Supabase Auth health probe — DB connectivity only. Service catalog rows on Monitor (`data.tools`) are domain data, not platform probes. No monitor unit tests in `tests/`. | Keep as REAL for V1 when live API + env are set. Add `VERCEL_*` to `.env.example`; consider auth on Vercel monitor route. Treat Settings webhook row as config-only, not health proof. |
| Repo Health Signals (Daily Build Check / CI) | NOT STARTED (in-app / automated) | Manual runbook only: `docs/AGENT_RUNBOOK.md:578-593` — Daily Build Health Check: human/agent-triggered (`knowledge/reference/workflow-entry-points.md:23`), `gh pr list --state open`, stale/duplicate scan, Build Log output; no timer (`knowledge/reference/workflow-entry-points.md:17`). CI exists but is not surfaced: `.github/workflows/ci.yml` (`npm test` + `npm run build` on push/PR). No app integration: zero `gh` / Octokit / Actions API usage in `src/`, `api/`, `lib/`, `scripts/`. `api/github/webhook.ts` ingests PR events for gate automation, not a repo-health dashboard signal. | No script, cron, or API route aggregates CI status, stale branches, or open-PR health into Monitor/Chief/Slack. Operators must run the runbook manually (or ad-hoc `gh`). Daily Slack turnover (repo-health section) remains NOT STARTED / V2. | Document as manual-only REAL process in runbooks, but NOT STARTED as a product signal. Phase 1: rely on Daily Build Health Check + GitHub UI/Actions; V2: optional in-app CI/stale-PR card fed by GitHub API. |

Slack is a real workspace and app surface for TrueCrew operators (they can post and read in `#truecrew-chief` today), but the dashboard does not yet send or consume any Slack messages. V1 operational honesty treats Slack as human-only coordination, not an approval or notification mechanism, until a `SLACK_WEBHOOK_URL` + `postToSlack()` path exists in code and is wired to Chief digests.

Integration Health (Monitor) is a real V1 capability when live API mode and server env are configured: Vercel and Supabase are checked via real probes and surfaced in Monitor and Chief. Repo health, by contrast, is not an in-product signal in V1; it is a manual operator workflow backed by GitHub Actions and the Daily Build Health Check runbook.

