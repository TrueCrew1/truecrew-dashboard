# Deprecated: Netlify config retained for history. Production hosting is via Vercel. This file should not be used for new deploys.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
